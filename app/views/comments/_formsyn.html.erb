<%= form_for([@syntagm, @syntagm.comments.build]) do |f| %>
  <div class="field">
    <%= f.label :body, "Add a Comment" %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
